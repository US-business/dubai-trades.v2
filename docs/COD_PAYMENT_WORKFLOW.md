# سير العمل للدفع عند الاستلام (Cash on Delivery - COD)

## نظرة عامة 📋

يوضح هذا المستند السيناريو الطبيعي لإدارة الدفع عند الاستلام في نظام التجارة الإلكترونية.

## تدفق الحالات 🔄

### 1. **إنشاء الطلب**
```
عند إنشاء طلب جديد مع الدفع عند الاستلام:
- Order Status: "pending" 
- Payment Status: "pending"
- Payment Type: "cash_on_delivery"
```

### 2. **معالجة الطلب**
```
عندما يبدأ تحضير الطلب:
- Order Status: "processing"
- Payment Status: "pending" (لا يتغير)
```

### 3. **شحن الطلب**
```
عند شحن الطلب:
- Order Status: "shipped"
- Payment Status: "pending" (لا يتغير حتى التسليم)
```

### 4. **تسليم الطلب**
```
عند تسليم الطلب للعميل:
- Order Status: "delivered"
- Payment Status: يمكن الآن تحديثه:
  - "paid" - إذا دفع العميل
  - "failed" - إذا رفض الدفع أو لم يدفع
```

## القواعد المهمة ⚠️

### للدفع عند الاستلام (COD):

1. **قبل التسليم:**
   - ❌ لا يمكن تغيير حالة الدفع من "pending"
   - ✅ يمكن تحديث حالة الطلب فقط

2. **بعد التسليم:**
   - ✅ يمكن تحديث حالة الدفع إلى:
     - `paid` - تم الدفع بنجاح
     - `failed` - فشل الدفع أو رفض العميل
     - `refunded` - استرداد المبلغ (نادراً)

3. **للدفع الإلكتروني:**
   - ✅ يمكن تحديث حالة الدفع في أي وقت
   - ✅ جميع الحالات متاحة

## واجهة المستخدم 🎨

### في قائمة الطلبات:
- عرض حالة الدفع مع ألوان مميزة
- إظهار "COD" للطلبات التي تستخدم الدفع عند الاستلام

### في تفاصيل الطلب:
- قسم "إدارة حالة الدفع" يظهر:
  - الحالة الحالية مع أيقونة ولون مميز
  - إمكانية تغيير الحالة (حسب القواعد)
  - تنبيهات وإرشادات للمستخدم

## التنبيهات والرسائل 💬

### قبل التسليم:
```
⚠️ لا يمكن تحديث حالة الدفع قبل تسليم الطلب
```

### بعد التسليم:
```
💡 يمكنك الآن تحديث حالة الدفع بعد التسليم
```

## الألوان المستخدمة 🎨

```typescript
const paymentStatusColors = {
  pending: "bg-amber-100 text-amber-800 border-amber-200",    // أصفر
  paid: "bg-emerald-100 text-emerald-800 border-emerald-200", // أخضر
  failed: "bg-red-100 text-red-800 border-red-200",          // أحمر
  refunded: "bg-indigo-100 text-indigo-800 border-indigo-200" // أزرق
}
```

## الملفات المحدثة 📁

### مكونات جديدة:
- `UpdatePaymentStatus.tsx` - إدارة حالة الدفع
- `COD_PAYMENT_WORKFLOW.md` - هذا المستند

### مكونات محدثة:
- `OrdersList.tsx` - ألوان منفصلة لحالة الدفع
- `PaymentInfoCard.tsx` - دمج مكون إدارة الدفع
- `orders.ts` - دالة `updatePaymentStatus()`

## نصائح للاستخدام 💡

1. **للإدارة:**
   - راقب الطلبات التي تم تسليمها ولم يتم تحديث حالة الدفع
   - استخدم الملاحظات لتسجيل أي مشاكل في الدفع

2. **للموظفين:**
   - تأكد من تحديث حالة الطلب إلى "delivered" أولاً
   - ثم حدث حالة الدفع حسب ما حدث فعلياً

3. **للتطوير:**
   - تم إضافة ملاحظات تلقائية عند تغيير حالة الدفع
   - يمكن إضافة إشعارات بريد إلكتروني في المستقبل

## مثال على التدفق الكامل 📝

```
1. عميل يطلب منتج → COD
   Order: pending, Payment: pending

2. تأكيد الطلب
   Order: processing, Payment: pending

3. شحن المنتج
   Order: shipped, Payment: pending

4. تسليم المنتج للعميل
   Order: delivered, Payment: pending

5. العميل يدفع المبلغ
   Order: delivered, Payment: paid ✅

أو

5. العميل يرفض الدفع
   Order: delivered, Payment: failed ❌
```

هذا النظام يضمن دقة إدارة المدفوعات ويوفر شفافية كاملة في تتبع حالة الدفع لكل طلب.
